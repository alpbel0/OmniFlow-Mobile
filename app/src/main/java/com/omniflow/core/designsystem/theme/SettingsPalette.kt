package com.omniflow.core.designsystem.theme

import androidx.compose.ui.graphics.Color

object SettingsPalette {
    val bgScreen = Color(0xFFF5F7F8)
    val primary = Color(0xFF007BFF)
    val textPrimary = Color(0xFF102033)
    val textSecond = Color(0xFF6F7F95)
    val surface = Color(0xFFFFFFFF)
    val border = Color(0xFFD9E2EC)
    val bgInput = Color(0xFFF0F4F8)
    val dangerRed = Color(0xFFFF3B30)

    // Icon Pastel Colors
    val bgAccount = Color(0xFFECEBFF)
    val tintAccount = Color(0xFF6366F1)

    val bgLock = Color(0xFFFFF1E6)
    val tintLock = Color(0xFFFF9F43)

    val bgMail = Color(0xFFFFE8EC)
    val tintMail = Color(0xFFFF4D4D)

    val bgBell = Color(0xFFFFF7E6)
    val tintBell = Color(0xFFFFC107)

    val bgMoon = Color(0xFFF3E8FF)
    val tintMoon = Color(0xFF9C27B0)

    val bgGlobe = Color(0xFFE3F2FD)
    val tintGlobe = Color(0xFF2196F3)

    val bgShield = Color(0xFFE8F5E9)
    val tintShield = Color(0xFF4CAF50)

    val bgBlock = Color(0xFFFFEBEE)
    val tintBlock = Color(0xFFF44336)

    val bgData = Color(0xFFE0F2F1)
    val tintData = Color(0xFF009688)

    val bgHelp = Color(0xFFE1F5FE)
    val tintHelp = Color(0xFF0288D1)

    val bgFeedback = Color(0xFFFFF3E0)
    val tintFeedback = Color(0xFFE65100)

    val bgInfo = Color(0xFFECEFF1)
    val tintInfo = Color(0xFF607D8B)
}
